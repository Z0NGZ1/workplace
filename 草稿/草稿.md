
``` mermaid
graph TD
    A[原始明细行为数据 （Tab3, 支付宝其他域）] --> B{1、行为预处理与特征化};
    B --> C(ODPS/CPU);
    C --> D{2、意图与实体识别};
    D -- LLM （可选, 辅助） --> E[结构化行为事件];
    D -- 规则/传统NLP --> E;
    E --> F{3、统计聚合与初步画像};
    F --> G(ODPS/CPU);
    G -- 统计特征 --> H{4、深度兴趣推理与扩展（LLM核心应用）};
    H -- 用户画像（含统计分） & 上下文 --> I[大语言模型（LLM）];
    I -- 推理出的深层兴趣/关联兴趣 --> J;
    J{5、兴趣与类目映射};
    J -- LLM（辅助）/ 规则 / Embedding --> K[用户兴趣-类目画像（含强度/生命周期）];
    K --> L[下游应用（推荐、运营）];

    subgraph 传统与规则处理为主-高效
        C
        G
    end

    subgraph LLM 增强与推理-深度
        I
    end

``` 